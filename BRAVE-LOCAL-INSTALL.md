# راهنمای نصب فونت‌آرا به صورت Local در Brave

این راهنما به شما کمک می‌کند تا فونت‌آرا را **بدون هیچ ارتباطی با اینترنت و سرورهای خارجی** روی مرورگر Brave نصب کنید.

## 🔒 تغییرات برای حفظ حریم خصوصی

تمامی ارتباطات با سرورهای خارجی حذف شده است:
- ✅ همه فونت‌ها به صورت local در extension هستند
- ✅ URL صفحه خوش‌آمدگویی به فایل local تغییر کرد
- ✅ URL فرم نظرسنجی حذف شد
- ✅ ارتباط با Google Fonts از Content Security Policy حذف شد
- ✅ همه داده‌ها در chrome.storage.local (محلی) ذخیره می‌شوند

## 📋 پیش‌نیازها

برای بیلد کردن extension نیاز دارید:
- Node.js (نسخه 18 یا بالاتر)
- pnpm (مدیر بسته)

## 🚀 مراحل نصب

### 1. نصب وابستگی‌ها

```bash
cd /Users/threehandss/Documents/GitHub/fontara
pnpm install
```

### 2. بیلد کردن برای Brave

```bash
pnpm run build:brave
```

این دستور یک پوشه به نام `build/brave-mv3-prod` می‌سازد که شامل extension آماده برای نصب است.

### 3. نصب در Brave

1. مرورگر Brave را باز کنید
2. آدرس `brave://extensions` را در نوار آدرس تایپ کنید و Enter بزنید
3. گزینه **Developer mode** را در گوشه بالا سمت راست فعال کنید
4. روی دکمه **Load unpacked** کلیک کنید
5. پوشه `build/brave-mv3-prod` را انتخاب کنید
6. تمام! فونت‌آرا نصب شد ✨

## 🔄 آپدیت کردن

هر زمان که تغییری در کد دادید:

```bash
pnpm run build:brave
```

سپس در صفحه `brave://extensions`، روی آیکون refresh کنار extension کلیک کنید.

## 🛡️ تاییدیه حریم خصوصی

### دسترسی‌های extension:

- **storage**: برای ذخیره تنظیمات شما به صورت local در دستگاهتان
- **activeTab**: برای دیدن URL تب فعال و اعمال فونت
- **tabs**: برای تشخیص زمانی که بین تب‌ها جابه‌جا می‌شوید
- **host_permissions** (`<all_urls>`): برای اعمال فونت در تمام سایت‌ها

### هیچ داده‌ای ارسال نمی‌شود:

- ❌ بدون ارتباط با سرورهای خارجی
- ❌ بدون ردیابی
- ❌ بدون analytics
- ❌ بدون ارسال داده
- ✅ همه چیز روی دستگاه شما باقی می‌ماند

## 🧪 تست کردن

برای اطمینان از عدم ارتباط با اینترنت:

1. Extension را نصب کنید
2. اینترنت خود را قطع کنید
3. Brave را restart کنید
4. فونت‌آرا باید کاملاً کار کند ✅

## 📝 یادداشت‌های مهم

- این نسخه کاملاً offline و local است
- هیچ اطلاعاتی از مرورگر شما خارج نمی‌شود
- تمام فونت‌ها داخل extension بسته‌بندی شده‌اند
- تنظیمات شما فقط در دستگاهتان ذخیره می‌شود

## 🆘 عیب‌یابی

### مشکل در نصب وابستگی‌ها:

```bash
# پاک کردن cache و نصب مجدد
rm -rf node_modules pnpm-lock.yaml
pnpm install
```

### مشکل در بیلد:

```bash
# پاک کردن پوشه build و بیلد مجدد
rm -rf build
pnpm run build:brave
```

### Extension کار نمی‌کند:

1. در `brave://extensions` بررسی کنید که extension فعال است
2. صفحه را refresh کنید (Ctrl+R یا Cmd+R)
3. Extension را reload کنید از صفحه extensions

## 🔧 توسعه (Development)

برای توسعه با hot reload:

```bash
pnpm run dev
```

این حالت برای تست و توسعه است و تغییرات به صورت خودکار اعمال می‌شود.

## 📦 بسته‌بندی برای توزیع

اگر می‌خواهید فایل ZIP برای توزیع بسازید:

```bash
pnpm run build:brave
```

فایل ZIP در پوشه `build` ساخته می‌شود که می‌توانید آن را با دوستانتان به اشتراک بگذارید.

## ⚖️ لایسنس

این پروژه Open Source است و می‌توانید آن را آزادانه استفاده و تغییر دهید.

## 🙏 تشکر

از سازندگان فونت‌های فارسی که اجازه استفاده از فونت‌هایشان را دادند:
- زنده یاد صابر راستی کردار
- امین عابدی
- صالح سوزنچی

---

**نکته امنیتی**: این نسخه خاص فونت‌آرا برای حفظ حریم خصوصی کامل طراحی شده است. هیچ داده‌ای از دستگاه شما خارج نمی‌شود.

